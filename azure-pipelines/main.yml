trigger:
  - main

- jobs:
  # - job: Windows
  #   pool:
  #     vmImage: windows-latest
  - job: Linux
    pool:
      vmImage: ubuntu-latest
    template: ./common/stages.yml
  # - job: macOS
  #   pool:
  #     vmImage: macOS-latest
